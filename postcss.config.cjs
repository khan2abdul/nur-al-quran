/**
 * PostCSS Configuration for Nur-Al-Quran
 * 
 * Processes Tailwind CSS and adds vendor prefixes
 * for cross-browser compatibility.
 */
module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
};
